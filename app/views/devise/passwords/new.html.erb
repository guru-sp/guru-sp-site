<h2>Esqueceu sua senha? Não entre em pânico!</h2>

<div class="inner">
  <p>Preencha o campo abaixo e lhe enviaremos um endereço para trocar sua senha.</p>
  <%= semantic_form_for resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post } do |form| %>
    <%= form.inputs do %>
        <%= form.input :email %>
    <% end %>
    <%= form.actions do %>
      <%= form.action :submit, :as => :button, :label => "Enviar email" %>
    <% end %>
  <% end %>
  <%= render :partial => "devise/shared/links" %>
</div>

