<h2>Registro de usuário</h2>

<div class="inner">
  <p>Registre-se no site do <strong>Guru-SP</strong> para aparecer na nossa listagem de membros.</p>
  <%= semantic_form_for resource, :as => resource_name, :url => registration_path(resource_name) do |form| %>
    <%= form.inputs do %>
      <%= form.input :email %>
      <%= form.input :password %>
      <%= form.input :password_confirmation %>
      <%= form.input :github, :hint => "Seu nome de usuário no github" %>
    <% end %>
    <%= form.buttons do %>
      <%= form.commit_button :label => "Registrar-se" %>
    <% end %>
  <% end %>

  <%= render :partial => "devise/shared/links" %>
</div>

