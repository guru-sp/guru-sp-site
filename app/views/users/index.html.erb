<h2>Membros</h2>

<div class="inner">
<p>O <strong>GURU-SP</strong> existe graças a seus membros. Faça parte dessa lista e ajude-nos a saber um pouco mais sobre você  <%= link_to "registrando-se aqui", new_user_registration_path %>.</p>
<ul class="list">
  <% for user in @users %>
  <li class="user list-item" >
    <div class="left">
      <%= gravatar_image_tag(user.email, :class => "avatar") %>
    </div>
    <div class="item">
      <h3><%= user.nickname %> (<%= user.name %>)
      <%= link_to "editar" , edit_user_registration_path if user == current_user %>
      </h3>
      <p>
      <strong>github: </strong><%= link_to user.github ,"https://github.com/#{user.github}" %> <br />
      <strong>site: </strong><%= link_to user.site, user.site %> <br />
      </p>
    </div>
  </li>
  <% end %>
</ul>
</div>

